// Production environment. Replaced in dev builds via angular.json fileReplacements.
export const environment = {
  production: true,
  // TODO: replace with the real public backend URL when it ships.
  apiUrl: 'https://api.chuckylab.example',
  defaultLang: 'en',
};
